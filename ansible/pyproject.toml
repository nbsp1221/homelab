[project]
name = "homelab-ansible"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "ansible-core==2.21.2",
]

[dependency-groups]
dev = [
  "ansible-lint==26.8.0",
]

[tool.uv]
package = false
