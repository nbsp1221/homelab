name: risuai

services:
  risuai-api-proxy:
    image: risuai-api-proxy:latest
    build:
      context: ./api-proxy
      dockerfile: Dockerfile
    container_name: risuai-api-proxy
    init: true
    restart: unless-stopped
    environment:
      API_PROXY_TOKEN: "${API_PROXY_TOKEN}"
    expose:
      - "8787"
    networks:
      - caddy-network
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8787/healthz').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  risuai:
    image: risuai-self-hosted:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: risuai
    init: true
    restart: unless-stopped
    environment:
      PROXY_STREAM_ALLOWED_HOSTS: "risuai-api-proxy"
      TRUST_PROXY: "1"
    expose:
      - "6001"
    networks:
      - caddy-network
    volumes:
      - ./save:/app/save
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:6001/manifest.json').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  caddy-network:
    name: caddy-network
    external: true
